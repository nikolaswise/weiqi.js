import { createGame, isOver, play, pass, areaScore } from './lib/game';
import { Position, Move } from './lib/records';

export default {
  createGame,
  isOver,
  play,
  pass,
  areaScore,
  Position,
  Move,
};
